%h1 Listing accounts

%table#balances
  %thead
    %tr
      %th Number
      %th Title
      %th Balance
      %th Account type
  %tbody
    - @accounts.each do |account|
      - if account.bookable == 1
        %tr
          %td= account.number
          %td= account.title
          %td= account.balance
          %td= account.account_type_id

%br/
= link_to 'New Account', new_account_path