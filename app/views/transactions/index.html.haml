%h1 
  = t('views.transaction.index.title')
  - if @account
    = "of account #{@account.number} #{@account.title}"

- if @account
  %p
    %strong= "Balance: #{number_to_currency(@account.balance)}"

= render 'table', :locals => @transactions

%br

= link_to t('views.transaction.defaults.new'), new_transaction_path
